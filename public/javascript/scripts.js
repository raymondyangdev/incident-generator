window.addEventListener('load', function () {
    // function generateTitle() {
    //     const app = document.querySelector('#app');
    //     const issue = document.querySelector('#issue');
    //     const error = document.querySelector('#error');
    //     const titleOutput = document.querySelector('#title');
    //     app.addEventListener('input', function () {
    //         titleOutput.value = `${app.value}`;
    //     });
    //     issue.addEventListener('input', function () {
    //         if (app.value) {
    //             titleOutput.value = `${app.value}`;
    //         }
    //         if (app.value && issue.value) {
    //             titleOutput.value = `${app.value} - ${issue.value}`;
    //         }
    //     });
    //     error.addEventListener('input', function () {
    //         if (app.value) {
    //             titleOutput.value = `${app.value}`;
    //         }
    //         if (app.value && issue.value) {
    //             titleOutput.value = `${app.value} - ${issue.value}`;
    //         }
    //         if (app.value && issue.value && error.value) {
    //             titleOutput.value = `${app.value} - ${issue.value} - ${error.value}`;
    //         }
    //         if (app.value && issue.value === '' && error.value) {
    //             titleOutput.value = `${app.value} - ${error.value}`;
    //         }
    //     });
    // }
    // generateTitle();
});
